---
import { getContent, getLang } from "@/contexts/PageContext";
import type { langType } from "@/types/languages";
import BtnCta from "./BtnCta.astro";
import bgHero from "/public/bg-hero.webp";

const CONTENT = getContent().hero;
const LANG = getLang() as langType;
---

<section
  id="home"
  class="relative min-h-dvh flex items-center justify-center text-white overflow-hidden"
>
  <!-- Background image -->
  <div class="absolute inset-0 z-0">
    <img
      src={bgHero.src}
      alt="Teraiz Software Consulting"
      class="w-full h-full object-cover"
    />

    <!-- Overlay -->
    <!-- <div class="absolute inset-0 bg-brand-primary/85"></div> -->

    <!-- Gradiente -->
    <div
      class="absolute inset-0
             bg-gradient-to-br
             from-brand-primary/30
             via-brand-primary/70
             to-brand-primary/100
             backdrop-blur-sm"
    >
    </div>
  </div>

  <!-- Content -->
  <div class="relative z-10 container mx-auto px-6">
    <div class="max-w-3xl mx-auto text-center">
      <!-- Title -->
      <h1
        class="font-display font-semibold leading-tight
           text-4xl sm:text-4xl md:text-5xl lg:text-6xl"
      >
        Acompañamiento técnico y
        <span class="block text-brand-secondary sm:inline">
          software a medida
        </span>
      </h1>

      <!-- Description -->
      <p class="mt-8 text-xl text-white/90 max-w-2xl mx-auto leading-relaxed">
        Transformamos ideas en sistemas escalables. De la estrategia al código
        en producción.
      </p>

      <!-- CTA -->
      <div class="mt-12">
        <BtnCta
          lang={LANG}
          textBtn="Solicitar consulta gratuita"
          sizeBtn="lg"
          classBtn="bg-brand-secondary hover:bg-brand-secondary/90 text-brand-primary px-10 py-4 text-lg font-semibold shadow-xl transition-all"
          visibleIcon={true}
        />
      </div>

      <!-- Trust -->
      <p class="mt-8 text-sm text-white/70">
        Análisis inicial gratuito · 20 minutos · Sin compromiso
      </p>
    </div>
  </div>
</section>
